name: lib_translations
description: Adds to your project root a new lib folder with all layers needed to add remote translations functionality.

version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev.42 <0.1.0"

vars: