
class AppConstants{
  static const String MAX_INCREMENT_COUNT_MSG = "You have reached the maximum increment count";
  static const String MIN_DECREMENT_COUNT_MSG = "You have reached the minimum decrement count";

  static const Duration ONE_SECOND = Duration(seconds: 1);
  static const Duration TWO_SECONDS = Duration(seconds: 2);
  static const Duration THREE_SECONDS = Duration(seconds: 3);
}